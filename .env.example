# Voyage Email Client - Environment Configuration

# Backend API URL (without trailing slash)
VITE_API_BASE=https://your-backend-url.com

# Optional: Custom IMAP settings (if using custom backend)
# IMAP_HOST=imap.purelymail.com
# IMAP_PORT=993

# Optional: Custom SMTP settings (if using custom backend)
# SMTP_HOST=smtp.purelymail.com
# SMTP_PORT=587